var xmpp = require('../lib/node-xmpp');

var cl = new xmpp.Client({ jid: "kartik@localhost/mac", password: "rustagi"});
cl.on('online', function() {
    console.log("ONLINE!!! YIHAA");
});
cl.on('stanza', function(stanza) {
    console.log(stanza);
});
